// use std::io;
// use std::cmp::Ordering;
// use rand::Rng;
extern crate rayon;

fn main() {

}

fn longest<'a>(_x: &str, _y: &str) -> String {
  let result = String::from("really long string");
  result
}

